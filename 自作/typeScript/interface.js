;
function show(p) {
    console.log("My name is " + p.name + ", and i am " + p.age);
}
/** interface 参数不能多或者少 顺序可变 用as可肆意代替interface **/
show({});
;
var isAdult = function (age) {
    return age >= 18;
};
var x;
x = funcB();
var n = String();
;
var me = { name: 'tangerine' };
var fMe = me;
fMe.age = 24; // 转换之后 可访问age属性
var list = [1, '2'];
